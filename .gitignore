.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
test/.build/

# Local installation credentials. Never commit the real file.
include/KilowattsSecrets.h

# kilowatts-mobile is its own separate Flutter project/git repo (Firebase
# app + installer web console) nested inside this directory for convenience
# during development. It must never be committed into the firmware repo.
kilowatts-mobile/

# Local Wi‑Fi credential helper (create `wifi_screen` from wifi_screen.template)
wifi_screen
